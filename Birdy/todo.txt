+ authentification services
+ tester les services en local
+ on delete cascade, if we delete a user, we delete the session and friendship


- implementer mongodb
- ajouter des ; dans chaque query
- add json error code to every json error (they are all 0)